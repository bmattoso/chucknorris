object TestLibs {
    const val junit = "junit:junit:${TestVersions.junit}"
    const val junitExt = "androidx.test.ext:junit:${TestVersions.junitExt}"
    const val espressoCore: String = "androidx.test.espresso:espresso-core:${TestVersions.espressoCore}"
    const val kotlinTest = "org.jetbrains.kotlin:kotlin-test-junit:${Versions.kotlin}"
}
