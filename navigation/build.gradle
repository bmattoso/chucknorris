apply from: "$rootDir/androidmodule.gradle"

dependencies {
    implementation(project(Modules.core))
    implementation(project(Modules.coreui))
    implementation(project(Modules.category))
    implementation(project(Modules.joke))
}
